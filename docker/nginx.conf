# nginx.conf
server {
    listen 80;
    server_name localhost;

    # 网站根目录
    root /usr/share/nginx/html;
    index index.html;

    # 配置路由：对于所有请求，都尝试匹配文件
    # 如果找不到文件，就回退到 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}